#!/bin/bash
source ${0%/*}/config.sh

echo ========================================
echo 安装 phpMyAdmin
cd ${build_dir}/${pcre_pkg}

wget ${downloads}/${phpmyadmin_url}
tar xvf ${downloads}/${phpmyadmin_pkg}.tar.gz -C /data0/htdocs/www/
cd /data0/htdocs/www/${phpmyadmin_pkg}
cp config.sample.inc.php config.inc.php
${nginx_install}/sbin/nginx -s reload
